1.摩尔定律已经过时，现在是Amdahl（阿姆达尔定律）主宰的时代，即当n(并行处理节点数）->无穷大时，加速比s=1/(1-a),a为可并行计算比例
2.线程安全源于共享可变状态，如果一个类在被多个线程访问时，能持续正确的行为，则说是线程安全的，前提是在单线程访问时就能进行正确的行为。
对于无状态的类，其本身就是安全的。
3.竞争条件 程序执行的结果依赖于运行时相关的时序和多线程的交替顺序，则产生了竞争条件
4.1
